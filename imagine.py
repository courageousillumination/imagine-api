def imagine(config):
    """
    Runs the imagine code.
    
    Be careful about running this. You probably want to lock it so
    you don't get a bunch of resource contention.
    """

    # Take the config, run some code, and then eventually save the image
    # and return the saved image location.
